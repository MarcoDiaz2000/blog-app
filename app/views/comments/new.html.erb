<%= form_with(model: [ @post, @post.comments.new ], local: true) do |form| %>
  <div class="field">
    <%= form.label :content, "Comentario" %>
    <%= form.text_area :content, class: "custom-input", placeholder: "Escribe tu comentario aquí" %>
  </div>
  <%= form.submit "Publicar Comentario", class: "custom-button" %>
<% end %>
